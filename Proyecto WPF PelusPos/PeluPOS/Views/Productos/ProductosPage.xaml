<Page x:Class="PeluPOS.Views.Productos.ProductosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PeluPOS.Views.Productos"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductosPage" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <StackPanel Grid.Row="0" Margin="0 0 0 16">
            <TextBlock Text="Productos"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>
            <TextBlock Text="Busca, crea y gestiona productos. Consulta sus ventas."
                       FontSize="12"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Tarjeta principal -->
        <Border Grid.Row="1"
                Background="{DynamicResource SurfaceBrush}"
                CornerRadius="12"
                Padding="16"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Barra superior: buscador + crear -->
                <Grid Grid.Row="0" Margin="0 0 0 12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Height="32"
                             Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}"
                             Background="{DynamicResource BackgroundBrush}"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             BorderBrush="{DynamicResource BorderBrush}"
                             BorderThickness="1"
                             Padding="10,0"/>

                    <Button Grid.Column="1"
                            Margin="12,0,0,0"
                            Content="+ Crear producto"
                            Width="150" Height="32"
                            Click="CreateProducto_Click"
                            Background="{DynamicResource PrimaryBrush}"
                            Foreground="{DynamicResource TextPrimaryDarkBrush}"
                            BorderThickness="0"/>
                </Grid>

                <!-- Lista tipo Clientes -->
                <ListView Grid.Row="1"
                          ItemsSource="{Binding ProductosFiltrados}"
                          SelectedItem="{Binding ProductoSeleccionado}"
                          Background="Transparent"
                          BorderThickness="0">

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Margin" Value="0,0,0,6"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Bd"
                                                Background="{DynamicResource BackgroundBrush}"
                                                BorderBrush="{DynamicResource BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="10">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Margin="5">
                                    <TextBlock Text="{Binding Nombre}"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="{Binding PrecioCompra, StringFormat=Compra: {0:0.00} €}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextSecondaryBrush}" />
                                        <TextBlock Text="   "
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextSecondaryBrush}" />
                                        <TextBlock Text="{Binding PrecioVenta, StringFormat=Venta: {0:0.00} €}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextSecondaryBrush}" />
                                        <TextBlock Text="   "
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextSecondaryBrush}" />
                                        <TextBlock Text="{Binding Stock, StringFormat=Stock: {0}}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextSecondaryBrush}" />
                                    </StackPanel>
                                </StackPanel>

                                <!-- Acciones -->
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="Ventas"
                                            Width="72" Height="28"
                                            Margin="0,0,8,0"
                                            Click="OpenVentas_Click"
                                            Background="{DynamicResource AccentBrush}"
                                            Foreground="{DynamicResource TextPrimaryBrush}"
                                            BorderThickness="0"
                                            Tag="{Binding Id}"/>

                                    <Button Content="Stock"
                                            Width="62" Height="28"
                                            Margin="0,0,8,0"
                                            Click="EditStock_Click"
                                            Background="{DynamicResource AccentBrush}"
                                            Foreground="{DynamicResource TextPrimaryBrush}"
                                            BorderThickness="0"
                                            Tag="{Binding}"/>

                                    <Button Content="Editar"
                                            Width="64" Height="28"
                                            Margin="0,0,8,0"
                                            Click="EditProducto_Click"
                                            Background="{DynamicResource AccentBrush}"
                                            Foreground="{DynamicResource TextPrimaryBrush}"
                                            BorderThickness="0"
                                            Tag="{Binding}"/>

                                    <Button Content="Eliminar"
                                            Width="72" Height="28"
                                            Click="DeleteProducto_Click"
                                            Background="{DynamicResource ErrorBrush}"
                                            Foreground="{DynamicResource TextPrimaryDarkBrush}"
                                            BorderThickness="0"
                                            Tag="{Binding Id}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
