<Window x:Class="PeluPOS.Views.Locales.NuevoLocalDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PeluPOS.Views.Locales"
        mc:Ignorable="d"
        Title="NuevoLocalDialog"  Width="420" Height="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Crear nuevo local"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0 0 0 12"/>

        <StackPanel Grid.Row="1" Margin="0 0 0 10">
            <TextBlock Text="Nombre"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="12"/>
            <TextBox Height="32"
                     Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}"
                     Background="{DynamicResource SurfaceBrush}"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     BorderThickness="1"
                     Padding="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0 0 0 10">
            <TextBlock Text="Dirección"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="12"/>
            <TextBox Height="30"
                     Text="{Binding Direccion, UpdateSourceTrigger=PropertyChanged}"
                     Background="{DynamicResource SurfaceBrush}"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     BorderThickness="1"
                     Padding="0,5,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="3"
                   Text="{Binding Error}"
                   Foreground="{DynamicResource ErrorBrush}"
                   FontSize="12"
                   Margin="0,6,0,0"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancelar"
                    Width="110" Height="36"
                    Margin="0,0,10,0"
                    Click="Cancelar_Click"
                    Background="{DynamicResource SurfaceBrush}"
                    Foreground="{DynamicResource TextPrimaryBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"/>
            <Button Content="Guardar"
                    Width="110" Height="36"
                    Click="Guardar_Click"
                    Background="{DynamicResource PrimaryBrush}"
                    Foreground="White"
                    BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
