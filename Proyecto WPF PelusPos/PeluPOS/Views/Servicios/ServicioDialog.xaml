<Window x:Class="PeluPOS.Views.Servicios.ServicioDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PeluPOS.Views.Servicios"
        mc:Ignorable="d"
        Title="ServicioDialog" Width="460" Height="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding DialogTitle}"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0 0 0 12"/>

        <StackPanel Grid.Row="1" Margin="0 0 0 10">
            <TextBlock Text="Nombre" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBox Height="30"
                     Text="{Binding VM.Nombre, UpdateSourceTrigger=PropertyChanged}"
                     Background="{DynamicResource SurfaceBrush}"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     BorderThickness="1"
                     Padding="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0 0 0 10">
            <TextBlock Text="Precio (€)" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBox Height="30"
                     Text="{Binding VM.Precio}"
                     Background="{DynamicResource SurfaceBrush}"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     BorderThickness="1"
                     Padding="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0 0 0 10">
            <TextBlock Text="Descripción" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBox Height="60"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Text="{Binding VM.Descripcion, UpdateSourceTrigger=PropertyChanged}"
                     Background="{DynamicResource SurfaceBrush}"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     BorderThickness="1"
                     Padding="10,6"/>
        </StackPanel>

        <TextBlock Grid.Row="4"
                   Text="{Binding VM.Error}"
                   Foreground="{DynamicResource ErrorBrush}"
                   FontSize="12"
                   Margin="0,6,0,0"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancelar"
                    Width="110" Height="36"
                    Margin="0,0,10,0"
                    Click="Cancelar_Click"
                    Background="{DynamicResource SurfaceBrush}"
                    Foreground="{DynamicResource TextPrimaryBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"/>
            <Button Content="Guardar"
                    Width="110" Height="36"
                    Click="Guardar_Click"
                    Background="{DynamicResource PrimaryBrush}"
                    Foreground="{DynamicResource TextPrimaryDarkBrush}"
                    BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
